# ┌────────────────────────────────────────────────────────────────────────────+
# │ [ ArcItemEditor 콘피그 파일입니다. ]
# │ [ 불필요한 메시지는 지우거나 해당 라인 앞에 '#'을 붇여서 주석 처리해주세요. ]
# │
# │ - ARCADIA 디스코드 : https://discord.gg/NanAFkRdww
# │ - ARCADIA 위키 : https://arcadia.imweb.me/#
# └────────────────────────────────────────────────────────────────────────────+

# ┌────────────────────────────────────────────────────────────────────────────+
# │
# │ [💬][ 메시지 설정 ]
# │
# └────────────────────────────────────────────────────────────────────────────+
message:
  normal:
    # 접두사
    prefix: "&6[ ITemEditor ] &f"

    # config를 리로드 했을 때 출력됨
    reload_config: "&a콘피그를 리로드했습니다."

  error:
    # 콘솔로 명령어를 입력했을 때 출력됨
    player_only: "&c플레이어만 입력이 가능한 커맨드입니다."

    # 권한이 없을 때 출력됨
    no_permission: "&c명령어를 사용할 권한이 없습니다."

    # 잘못된 명령어를 입력했을 때 출력됨
    wrong_command: "&c잘못된 명령어입니다."

  main:
    # 이름 변경시 출력됨
    name_edit: "&a아이템의 이름이 변경되었습니다."

    # 로어 변경시 출력됨
    lore_edit: "&a아이템의 로어가 변경되었습니다."

    # 커스텀 모델 데이터 변경시 출력됨
    custommodeldata_edit: "&a아이템의 커스텀모델데이터 값이 &9&l{custommodeldata}번 &a으로 변경되었습니다."

    # 인챈트 추가시 출력됨
    enchant_add: "&a아이템에 인챈트 &9&l{enchantment} Lv.{level}&a를 추가했습니다."

    # 인챈트 삭제시 출력됨
    enchant_delete: "&a아이템에 인챈트 &9&l{enchantment}&a를 제거했습니다."

    # 내구도 무한을 활성화시 출력됨
    unbreakable_edit: "&a아이템의 내구도 무한이 활성화 되었습니다."

    # 내구도 무한을 비활성화시 출력됨
    breakable_edit: "&a아이템의 내구도 무한이 비활성화 되었습니다."